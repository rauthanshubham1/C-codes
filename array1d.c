#include <stdio.h>
int main()
{
    int i;
    int marks[4] = {0, 1, 2, 3};    // 0 is 0th term, 1 is 1nd term an so on//
    // for (i = 0; i < 4; i++)
    // {
    // printf("Enter the marks you want to store in %d\n", i);
    // scanf("%d", &marks[i]);
    // }
    for (i = 0; i < 4; i++)
     {
        printf("Value of %d elements is %d\n", i , marks[i]);
     }
    printf(typedef(i));
    return 0;
}
