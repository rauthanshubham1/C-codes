#include <stdio.h>
#include <string.h>
int main()
{
    char s1[100] = "harry";     // size array ka itna jyada bcoz dono string concatinate hoke s1 ma save hoge//
    char s2[] = "ravi";
    char s3[54]; 
    // puts(strcat(s1,s2));
    // printf("The length of s1 string is %lu\n", strlen(s1));
    // printf("The length of s2 string is %lu\n", strlen(s2));
    // printf("The reverse of s1 string is");
    // puts(strrev(s1));                       // this will show error bcoz strrev is no longer in c library
    // // printf("The reverse of s2 string is %d\n", strrev(s2));
    // printf("The strcmp of s1 and s2 is %d\n",strcmp(s1,s2));
    // strcpy(s3, strcat(s1,s2));  
    // puts(s3);
    
    return 0;
}